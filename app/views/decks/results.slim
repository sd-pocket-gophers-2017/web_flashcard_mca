h2 = @deck.name

p = "You've completed a round of #{@deck.name}. You answered #{@first_try}/#{@deck.cards.count} cards correctly on the first try.  You submitted #{@deck.guesses.count} total guesses before each card was answered correctly."
