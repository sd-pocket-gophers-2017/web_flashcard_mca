h2 = @round.deck.name

p = "You've completed a round of #{@round.deck.name}. You answered #{@first_try}/#{@round.deck.cards.count} cards correctly on the first try.  You submitted #{@round.guesses.count} total guesses before each card was answered correctly."
